
//const user = localStorage.getItem('token')

const userReducer =(state = {name : "ram"}, action)=>{
    switch(action.type){

        default :
        return state
    }
}

export default userReducer